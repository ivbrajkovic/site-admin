import { ContextModalProps } from '@mantine/modals';

import { CreateForm } from 'apps/Shortlink/components/CreateForm/CreateForm';

export const CreateModal = (_props: ContextModalProps) => <CreateForm />;
